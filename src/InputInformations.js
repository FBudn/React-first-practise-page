//import InputDiv from "./Input";
//import CheckBox from "./CheckBox";
//import Buttons from "./Buttons";
//import { useState } from "react";

const InputInfo = () => {
  return <div></div>;
};

export default InputInfo;

//tu funkcja onClicka buttona

/* 
 <div className="flex h-screen w-full">
      <div>{emai1l}</div>
      <div>{password}</div>
      <div>{checkboxBoolean}</div>
    </div>
    function getInfo() {
    return <InputInfo />;
  }
  const [11email, setEma1il] = useState(
    document.getElementById("e1mailInput").value
  );
  const [password, setPassword] = useState(
    document.getElementById("passwordInput").value
  );
  const checkboxBoolean = document.getElementById("rememberPasswordID").checked;

   <div className="flex h-1/2 w-1/2">
      `${emai1l}` `${password}` `${checkboxBoolean}`
    </div>
    */

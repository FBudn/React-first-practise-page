//import useFetch from "/.useFetch/";
const InputInfo = () => {
  return (
    <div
      id="inputInfoDIV"
      className="hidden absolute w-screen h-screen bg-green-400 left-0 justify-center align-center items-center text-center"
    ></div>
  );
};

export default InputInfo;

/* 
const { data: inputData } = useFetch("http://localhost:3000/#");
*/
